Deploy steps
02 June 2016

per https://consul.stanford.edu/display/dlssdevops/Capistrano+Template

- add :deployment group to Gemfile
- $ bundle install
- $ capify

- mimic Authorial London
  -> fails cap staging deploy:check
  - stumped




/*
 * database related
*/

# locally
$ pg_dump -F c -n public -c -x -O -d kb_test -f /kb/dump/kb_test_20160601.backup

# on cidr-kindred-britain-edit
$ pg_restore -d kb_edit -c -O dumps/kb_test_20160601.backup

# get copy back to local for testing
$ pg_dump -F c -n public -c -x -O -d kb_edit -f dumps/kb_edit_20160602.backup
$ pg_restore -d kbtester -c -O /kb/dump/kb_edit_20160602.backup

  -F format=custom
  -n schema=public
  -c clean
  -x no privileges
  -O no owner


  # on orbis-prod
  $ pg_dump -U postgres -p 5432 -F c -n public -c -x -O -d kblean -f ~/pgbackup/kindred_full_20160324.backup
